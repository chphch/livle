<% button = '계정잠금 해제하기' %>
<% redirect_url = unlock_url(@resource, unlock_token: @token) %>
<% message = '잘못된 로그인 시도로 고객님의 계정이 일시적으로 잠겼습니다.
아래 버튼을 클릭해 잠금을 해제한 후 이용해주세요' %>

<%= render partial: "mailer-form", locals: {button: button, redirect_url: redirect_url, message: message, notice: 'none'} %>