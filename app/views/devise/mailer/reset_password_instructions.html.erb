<% button = '비밀번호 변경하기' %>
<% redirect_url = edit_password_url(@resource, reset_password_token: @token) %>
<% message = '아래 버튼을 눌러 새 비밀번호를 설정해주세요' %>

<%= render partial: "mailer_form", locals: {button: button, redirect_url: redirect_url, message: message, notice: 'none'} %>
