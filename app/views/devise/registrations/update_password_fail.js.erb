(function () {
  var error_message = $('.error-message');
  error_message.empty();
  <% @user.errors.messages.each do |key, value| %>
    <% value.each do |v| %>
      error_message.append("<%= "#{key} #{v}" %><br>");
    <% end %>
  <% end %>
}());
