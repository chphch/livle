(function () {
  var error_message = $('.error-message');
  error_message.empty();
  <% @user.errors.messages.each do |message| %>
    <% value.each do |v| %>
      error_message.append("<%= "#{key} #{v}" %><br>");
    <% end %>
  <% end %>
}());
