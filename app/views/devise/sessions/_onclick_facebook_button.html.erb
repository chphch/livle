<%
# Deprecation by jaeseong
# text/html response cannot be handled by ajax
if false %>
<script>
    function onClickFacebookLoginButton() {
        $.ajax({
            method: "get",
            url: "<%= url %>",
            crossDomain: true,
            dataType: 'jsonp',
            xhrFields: {withCredentials: true}
        });
        return false;
    }

    // for facebook login callback function
    var isfacebookLoginCallbackTrue = <%= remote %>;
</script>
<% end %>
