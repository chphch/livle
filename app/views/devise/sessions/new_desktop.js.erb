$('body').append("<%=j render partial: 'new_modal_desktop'%>");
var loginModal = $('#login-modal');
loginModal.modal();
$('._big-modal').css('margin-top',
    ($(window).height() - $('._big-modal').height())/2);
loginModal.on('hide.bs.modal', function() {loginModal.remove()});
