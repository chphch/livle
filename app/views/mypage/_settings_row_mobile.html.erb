<%= link_to url, method: method, onclick: defined?(disabled) ? "return false" : "", remote: defined?(remote) ? true : false do %>
  <div <%= defined?(id) ? "id=#{id}" : "" %> class="row mypage-settings-row">
    <div class="col-xs-offset-1 col-xs-10">
      <%= tag %>
    </div>
    <div class="col-xs-1">
      <%= image_tag 'icon_arrow_right_black.png' %>
    </div>
  </div>
<% end %>
