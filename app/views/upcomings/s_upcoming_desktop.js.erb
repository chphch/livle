$('#upcomings-card-list').append('<%= j render partial: "ticket_card_desktop", collection: @upcomings, as: :upcoming %>');
<% if @upcomings.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @upcomings %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
